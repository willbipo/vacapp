package com.vacapp.calendario;

import org.springframework.stereotype.Service;

/**
 * API pública del módulo Calendario.
 * Otros módulos deben interactuar con Calendario únicamente a través de esta clase.
 */
@Service
public class CalendarioService {
    // Actualmente el calendario es autocontenido y no expone API a otros módulos.
}
